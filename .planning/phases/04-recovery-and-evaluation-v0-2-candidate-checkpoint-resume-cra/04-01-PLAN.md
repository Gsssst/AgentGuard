---
phase: 04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/agentguard/checkpoint/__init__.py
  - src/agentguard/checkpoint/model.py
  - src/agentguard/checkpoint/codec.py
  - src/agentguard/checkpoint/store.py
  - src/agentguard/__init__.py
  - tests/unit/test_checkpoint.py
  - tests/unit/test_checkpoint_store.py
autonomous: true
requirements:
  - CHECKPOINT-01
  - CHECKPOINT-02
---

<objective>
Create the typed, schema-versioned local JSON checkpoint foundation with strict decoding and atomic replacement. This plan must be usable independently by the Runtime integration plan and must never execute a Tool while loading or validating a checkpoint.
</objective>

<tasks>

<task type="implementation">
  <name>Define checkpoint DTOs, lifecycle, and validation errors</name>
  <files>src/agentguard/checkpoint/model.py</files>
  <read_first>
    src/agentguard/domain/state.py
    src/agentguard/domain/actions.py
    src/agentguard/domain/results.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-CONTEXT.md
  </read_first>
  <action>Implement `CheckpointLifecycle` with values `active`, `recoverable`, `completed`, and `failed`; a typed `Checkpoint` dataclass containing `schema_version=1`, `run_id`, serialized RunState projection fields, `max_steps`, `event_position`, `resume_attempt`, lifecycle, and optional diagnostic action/result metadata; and distinct `CheckpointCorruptError`, `CheckpointValidationError`, `UnsupportedCheckpointVersionError`, and `CheckpointSerializationError` exceptions. Validate non-empty run IDs, non-negative step/event/resume counters, supported lifecycle/schema values, and required runtime configuration in `__post_init__`.</action>
  <verify>Importing the checkpoint package succeeds; invalid IDs, negative counters, unsupported lifecycle values, and unsupported schema versions raise the named errors; a valid Checkpoint instance has schema_version 1 and lifecycle `active` by default.</verify>
  <acceptance_criteria>
    - `src/agentguard/checkpoint/model.py` defines all four lifecycle values and all four distinct exception classes.
    - `Checkpoint(run_id="r", ...)` validates successfully and `Checkpoint(run_id="", ...)` raises `ValueError` or `CheckpointValidationError`.
    - No checkpoint model imports a database, Redis client, pickle, or external runtime package.
  </acceptance_criteria>
</task>

<task type="implementation">
  <name>Implement explicit JSON codec and strict decoder</name>
  <files>src/agentguard/checkpoint/codec.py</files>
  <read_first>
    src/agentguard/checkpoint/model.py
    src/agentguard/events/model.py
    src/agentguard/domain/actions.py
    src/agentguard/domain/results.py
    src/agentguard/domain/state.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-RESEARCH.md
  </read_first>
  <action>Implement `encode_checkpoint(checkpoint) -> dict[str, object]`, `decode_checkpoint(raw) -> Checkpoint`, `dumps_checkpoint(checkpoint) -> str`, and `loads_checkpoint(text) -> Checkpoint`. Encode `CallTool` and `Finish` with explicit action tags, ToolResult status/failure/retry fields with enum values, bounded RunState history, sorted JSON keys, and `ensure_ascii=False`. Require all mandatory top-level/state/runtime keys before construction; map JSON syntax errors to `CheckpointCorruptError`, missing/type-invalid fields to `CheckpointValidationError`, wrong schema version to `UnsupportedCheckpointVersionError`, and non-JSON-compatible values to `CheckpointSerializationError`. Never use pickle, `__dict__`, repr, or default filling for required fields.</action>
  <verify>Round-trip a RunState containing both CallTool and Finish history plus success/failure/timeout ToolResults; mutate JSON ordering and confirm equivalent decode; remove required keys, corrupt JSON, change schema_version, and pass a custom object as ToolResult.value to confirm each expected error class.</verify>
  <acceptance_criteria>
    - `json.loads(dumps_checkpoint(cp))` returns a plain JSON object with `schema_version: 1` and stable sorted-key output.
    - Round-trip preserves run_id, step, status, history_limit, recent history order, ToolResult status, failure kind, attempts, and resume metadata.
    - `loads_checkpoint("{")` raises `CheckpointCorruptError`; version 99 raises `UnsupportedCheckpointVersionError`; missing `state` raises `CheckpointValidationError`.
    - Unsupported ToolResult values fail explicitly instead of being stringified.
  </acceptance_criteria>
</task>

<task type="implementation">
  <name>Implement atomic local CheckpointStore and public exports</name>
  <files>src/agentguard/checkpoint/store.py, src/agentguard/checkpoint/__init__.py, src/agentguard/__init__.py</files>
  <read_first>
    src/agentguard/checkpoint/model.py
    src/agentguard/checkpoint/codec.py
    src/agentguard/events/sinks.py
    src/agentguard/events/__init__.py
    src/agentguard/__init__.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-RESEARCH.md
  </read_first>
  <action>Implement `CheckpointStore(root: str | Path)`, `path_for(run_id)`, `save(checkpoint, path=None)`, and `load(path)`. Normalize paths with pathlib, create the root directory, write JSON to a same-directory tempfile, flush and `os.fsync` it, then call `os.replace`; remove only the temporary file on failure and leave the previous canonical file untouched. Use `{run_id}.json` as the default filename, preserve the lifecycle field, and re-export stable checkpoint DTO/codec/store/error names through `checkpoint.__init__` and the root package.</action>
  <verify>Using pytest `tmp_path`, save and load a checkpoint, assert parent directory/file creation and parseable JSON, overwrite and assert the final file is valid, monkeypatch replacement failure and assert the prior file bytes remain unchanged, and verify `path_for("run-1")` ends in `run-1.json`.</verify>
  <acceptance_criteria>
    - `CheckpointStore.save()` never opens the canonical path in truncate mode before `os.replace`.
    - A failed save leaves the previous valid checkpoint readable and leaves no `.tmp` file in the destination directory.
    - `from agentguard import Checkpoint, CheckpointStore, CheckpointValidationError` succeeds without import side effects.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `PYTHONPATH=src pytest -q tests/unit/test_checkpoint.py tests/unit/test_checkpoint_store.py` passes.
- JSON output is deterministic and contains no pickle or arbitrary object serialization.
- Corrupt, incomplete, or unsupported checkpoint data fails before any Runtime/Tool dependency is invoked.
</verification>

<success_criteria>
- A schema-versioned Checkpoint can be encoded, atomically saved, loaded, and strictly validated using only Python standard library facilities.
- The canonical file remains recoverable after a failed replacement attempt.
</success_criteria>
