---
phase: 04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra
plan: 02
type: implementation
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/agentguard/runtime/engine.py
  - src/agentguard/events/model.py
  - src/agentguard/events/sinks.py
  - src/agentguard/reporting/report.py
  - tests/integration/test_recovery_scenarios.py
  - tests/unit/test_report.py
autonomous: true
requirements:
  - RECOVERY-01
  - RECOVERY-02
  - RECOVERY-03
---

<objective>
Integrate post-step checkpointing, deterministic crash injection, and explicit resume into the existing Runtime loop while preserving timeout, retry, loop-guard, event, and terminal-result semantics.
</objective>

<tasks>

<task type="implementation">
  <name>Extend recovery event vocabulary and event sequence metadata</name>
  <files>src/agentguard/events/model.py, src/agentguard/events/sinks.py</files>
  <read_first>
    src/agentguard/events/model.py
    src/agentguard/events/sinks.py
    src/agentguard/runtime/engine.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-CONTEXT.md
  </read_first>
  <action>Add event types for checkpoint written, resume started, duplicate-possible replay, and recovery rejected using stable snake_case values. Add an optional monotonic `sequence` field to RuntimeEvent or an equivalent runtime-assigned field while preserving existing `to_dict()` keys and JSONL compatibility. Ensure recovery event data carries `resume_attempt`, `duplicate_possible`, and checkpoint path/status where applicable; keep sinks synchronous and append-only.</action>
  <verify>Existing Phase 1–3 tests continue to pass; new events serialize as JSON; a fresh sink can append recovery events without changing existing event order or event_type values.</verify>
  <acceptance_criteria>
    - Existing event JSON shape remains parseable and current tests for `run_started`, `tool_succeeded`, `run_finished`, and JSONL still pass.
    - Recovery events include the same run_id and explicit resume_attempt in their data.
    - Sequence values, if added, are monotonic within one logical run and do not reset to an already-used value on resume.
  </acceptance_criteria>
</task>

<task type="implementation">
  <name>Add checkpoint hooks, SimulatedCrash, and explicit Runtime.resume</name>
  <files>src/agentguard/runtime/engine.py</files>
  <read_first>
    src/agentguard/runtime/engine.py
    src/agentguard/runtime/tool.py
    src/agentguard/runtime/router.py
    src/agentguard/checkpoint/model.py
    src/agentguard/checkpoint/codec.py
    src/agentguard/checkpoint/store.py
    src/agentguard/events/model.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-RESEARCH.md
  </read_first>
  <action>Extend Runtime with optional CheckpointStore/path and disabled-by-default crash hook. After each complete Tool step (executor result, `state.record`, and `state.step += 1`) invoke a deterministic hook; when configured for `after_tool_before_checkpoint`, raise `SimulatedCrash` before save so the previous checkpoint remains intact. Save post-step checkpoints and terminal lifecycle updates. Implement `async resume(checkpoint_path: str | Path, router: Router)` that loads/validates the entire checkpoint before emitting recovery events or calling Router/Tool, reconstructs RunState and runtime max_steps, preserves run_id, increments resume_attempt, marks duplicate_possible for the unconfirmed step, and enters the same internal loop. Do not scan checkpoint directories or auto-resume from `run()`; reuse ToolExecutor and LoopGuard behavior.</action>
  <verify>Run clean execution and assert checkpoints after each complete step; inject `after_tool_before_checkpoint`, catch `SimulatedCrash`, assert the previous checkpoint bytes remain valid, resume explicitly, assert same run_id/resume_attempt increment and possible duplicate execution, and verify corrupt/version-invalid checkpoints fail before a side-effect counter increments.</verify>
  <acceptance_criteria>
    - Normal `Runtime.run()` with no checkpoint configuration behaves exactly as before and all 53 existing tests remain green.
    - A checkpoint is written only after `state.record()` and step increment; the crash hook can leave the previous checkpoint as the last valid state.
    - `resume()` validates before any Router or Tool invocation and raises the distinct checkpoint errors for corrupt, incomplete, or unsupported input.
    - Resume appends events with the original run_id, incremented resume_attempt, and `duplicate_possible=true` for the crash window.
    - Runtime does not discover or execute stale checkpoint files unless `resume()` is explicitly called.
  </acceptance_criteria>
</task>

<task type="implementation">
  <name>Extend reliability reports with recovery evidence</name>
  <files>src/agentguard/reporting/report.py, tests/unit/test_report.py</files>
  <read_first>
    src/agentguard/reporting/report.py
    src/agentguard/events/model.py
    src/agentguard/runtime/engine.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-CONTEXT.md
  </read_first>
  <action>Preserve all existing ReliabilityReport fields and add machine-readable recovery metrics: checkpoint writes/successes, recovery success, duplicate-possible Tool executions, crash-to-recovery steps, final-state correctness, and resume attempt count. Derive values from event evidence and terminal RunResult rather than hidden counters. Keep `evidence_consistent` and existing JSON serialization stable; represent unavailable metrics explicitly as zero/false/null according to a documented rule.</action>
  <verify>Update report unit tests for clean, loop-detected, recovery, and truncated event streams; assert existing fields remain unchanged, recovery fields match event data, and missing terminal evidence still makes `evidence_consistent` false.</verify>
  <acceptance_criteria>
    - `build_report(...).to_dict()` contains both every V0.1 field and the documented Phase 4 recovery fields.
    - A recovery trace with one resume attempt and one duplicate-possible execution reports those exact counts.
    - A report never claims recovery success or final-state correctness when the required evidence is missing.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `PYTHONPATH=src pytest -q` passes all existing and new tests.
- Integration tests demonstrate clean checkpointing, crash-before-checkpoint, explicit resume, duplicate evidence, and validation-before-side-effect rejection.
- Existing CLI JSONL behavior and V0.1 reliability report compatibility remain intact.
</verification>

<success_criteria>
- The same Runtime execution path supports normal runs and explicit recovery.
- At-least-once replay is observable and honestly reported; no exactly-once claim is introduced.
</success_criteria>
