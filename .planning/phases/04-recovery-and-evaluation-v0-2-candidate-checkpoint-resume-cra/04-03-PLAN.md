---
phase: 04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra
plan: 03
type: implementation
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/agentguard/evaluation/__init__.py
  - src/agentguard/evaluation/scenarios.py
  - src/agentguard/evaluation/runner.py
  - src/agentguard/__init__.py
  - tests/unit/test_evaluation.py
  - tests/integration/test_recovery_scenarios.py
  - learning/zh-CN/04-checkpoint-recovery.md
  - learning/en/04-checkpoint-recovery.md
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
---

<objective>
Create a shared deterministic scenario registry and evaluation runner for clean completion, crash-and-resume, and corrupt-checkpoint rejection, then document measured recovery semantics in bilingual learning notes.
</objective>

<tasks>

<task type="implementation">
  <name>Define immutable scenario registry and three baseline scenarios</name>
  <files>src/agentguard/evaluation/scenarios.py, src/agentguard/evaluation/__init__.py, src/agentguard/__init__.py</files>
  <read_first>
    src/agentguard/runtime/router.py
    src/agentguard/runtime/tool.py
    src/agentguard/domain/state.py
    src/agentguard/checkpoint/store.py
    src/agentguard/runtime/engine.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-CONTEXT.md
  </read_first>
  <action>Implement an immutable `ScenarioDefinition` with name, description, fresh factory, expected terminal predicate, fault configuration, and metric names; implement a registry with explicit `register`, `get`, and `all` operations and a clear unknown-scenario error. Register exactly three initial deterministic factories: clean completion, Tool-complete/crash-before-checkpoint then explicit resume, and corrupt-checkpoint rejection. Factories must create fresh Router, ToolRegistry/ToolExecutor, RunState, checkpoint path, event sink, and side-effect counters per invocation.</action>
  <verify>Unit-test registry lookup, duplicate-name rejection, fresh-object identity, stable scenario names, and expected terminal predicates; assert no factory shares mutable counters or Router instances between calls.</verify>
  <acceptance_criteria>
    - Registry exposes the three named scenarios and rejects an unknown name without running a Tool.
    - Calling one scenario factory twice returns distinct mutable runtime objects and isolated counters.
    - Scenario definitions contain expected terminal-state and reliability-metric declarations readable without executing the scenario.
  </acceptance_criteria>
</task>

<task type="implementation">
  <name>Implement evaluation runner and JSON result projection</name>
  <files>src/agentguard/evaluation/runner.py, tests/unit/test_evaluation.py, tests/integration/test_recovery_scenarios.py</files>
  <read_first>
    src/agentguard/evaluation/scenarios.py
    src/agentguard/runtime/engine.py
    src/agentguard/reporting/report.py
    src/agentguard/checkpoint/model.py
    src/agentguard/events/model.py
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-RESEARCH.md
  </read_first>
  <action>Implement an async scenario runner that resolves a registered factory, runs the Runtime, catches only `SimulatedCrash` for the crash scenario, invokes explicit `resume()` with the same checkpoint path, builds a reliability report, and returns JSON-serializable output. Compute checkpoint write count/success, recovery success, duplicate-possible Tool executions, crash-to-recovery step count, and final-state correctness from events/report predicates. Keep runs sequential and deterministic; expose `run_scenario(name)` and optional `run_all()` without adding a service or benchmark dependency.</action>
  <verify>Run each of the three scenarios and assert JSON output contains the scenario name, terminal status/reason, metrics, ordered events or report reference, and no unverified model/token/cost metrics. Assert crash scenario completes after resume with duplicate_possible evidence and corrupt scenario returns a safe rejection without side effects.</verify>
  <acceptance_criteria>
    - Clean scenario reports completed with zero recovery attempts and final-state correctness true.
    - Crash scenario reports recovery success true, same run_id, resume_attempt 1, duplicate_possible count at least 1, and expected final state.
    - Corrupt scenario reports safe rejection, preserves checkpoint bytes, and side-effect counter remains zero.
    - `json.dumps(result)` succeeds without custom encoders.
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Write bilingual checkpoint/recovery learning notes</name>
  <files>learning/zh-CN/04-checkpoint-recovery.md, learning/en/04-checkpoint-recovery.md</files>
  <read_first>
    learning/zh-CN/02-tool-timeout.md
    learning/en/02-tool-timeout.md
    learning/zh-CN/03-loop-detection.md
    learning/en/03-loop-detection.md
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-CONTEXT.md
    .planning/phases/04-recovery-and-evaluation-v0-2-candidate-checkpoint-resume-cra/04-RESEARCH.md
    tests/integration/test_recovery_scenarios.py
  </read_first>
  <action>Document the post-step atomic checkpoint boundary, `flush/fsync/os.replace` rationale, explicit resume API, validation-before-side-effect rule, deterministic `SimulatedCrash`, at-least-once duplicate semantics, same run_id plus resume_attempt evidence, three scenario results, and limitations. Keep Chinese and English facts aligned and explicitly avoid exactly-once, process-level termination, distributed durability, and model-quality claims.</action>
  <verify>Review both notes side by side against the implemented tests and event traces; each note names the same scenarios, observed duplicate behavior, rejection behavior, and deferred capabilities.</verify>
  <acceptance_criteria>
    - Both files explain what checkpoint stores, when it is written, how resume is invoked, and why duplicate execution is possible.
    - Both files cite concrete test commands/results and distinguish verified behavior from assumptions or deferred work.
    - Neither file claims exactly-once execution or production-grade power-loss durability.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `PYTHONPATH=src pytest -q` passes all unit and integration tests.
- Running all registered scenarios produces deterministic JSON-serializable reliability results.
- Chinese and English learning notes match the observed implementation and event evidence.
</verification>

<success_criteria>
- One scenario definition drives both tests and evaluation output for clean, crash/replay, and corrupt-checkpoint paths.
- Phase 4 produces auditable recovery metrics grounded in actual event evidence.
</success_criteria>
