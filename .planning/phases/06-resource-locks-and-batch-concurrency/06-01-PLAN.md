---
phase: 06-resource-locks-and-batch-concurrency
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/agentguard/runtime/resources.py
  - src/agentguard/runtime/tool.py
  - src/agentguard/runtime/permission.py
  - src/agentguard/runtime/__init__.py
  - src/agentguard/__init__.py
  - tests/unit/test_resource_locks.py
autonomous: true
must_haves:
  truths:
    - "Resource declarations are immutable, non-empty, and consistent with Tool capabilities."
    - "Read locks share, write/destructive locks exclude, and waiting writers receive priority."
    - "Multi-resource acquisition is sorted, timeout-bounded, and releases partial state."
  artifacts:
    - path: "src/agentguard/runtime/resources.py"
      provides: "ResourceAccess and ResourceLockManager"
    - path: "tests/unit/test_resource_locks.py"
      provides: "Concurrency and deadlock-prevention coverage"
---

<objective>
Define resource access metadata and a deterministic, deadlock-resistant process-local read/write lock manager.
</objective>

<tasks>

<task type="implementation">
  <name>Define resource access metadata and validate Tool declarations</name>
  <files>src/agentguard/runtime/resources.py, src/agentguard/runtime/tool.py, src/agentguard/runtime/permission.py</files>
  <action>Add `ResourceAccess` with read/write/destructive modes, immutable resource mappings on Tool, minimal resource-ID normalization, and registration-time validation that each mode is covered by Tool capabilities. Preserve empty resources for old callers.</action>
  <verify>Tests accept valid declarations, reject blank IDs/unknown modes and capability mismatches, collapse duplicate mapping keys naturally, and preserve no-resource Tool compatibility.</verify>
</task>

<task type="implementation">
  <name>Implement write-priority ResourceLockManager</name>
  <files>src/agentguard/runtime/resources.py</files>
  <action>Implement async acquire/release or context-manager APIs for shared reads and exclusive writes/destructive access, sorted all-resource acquisition, write-priority admission, monotonic lock timeout, partial-acquisition cleanup, and no lock upgrade.</action>
  <verify>Unit tests cover read/read overlap, write exclusion, writer priority, sorted multi-resource acquisition, timeout, cancellation, and release after exceptions.</verify>
</task>

</tasks>

<success_criteria>
- Resource metadata is immutable and capability-consistent.
- Lock acquisition is deterministic, write-priority, timeout-bounded, and leak-free.
</success_criteria>
