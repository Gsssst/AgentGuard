---
phase: 06-resource-locks-and-batch-concurrency
plan: 02
type: implementation
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/agentguard/runtime/engine.py
  - src/agentguard/runtime/tool.py
  - src/agentguard/domain/results.py
  - src/agentguard/__init__.py
  - tests/unit/test_tool_execution.py
  - tests/integration/test_batch_concurrency.py
autonomous: true
must_haves:
  truths:
    - "Locks are acquired before Tool side effects and released on every exit path."
    - "execute_batch runs independent actions concurrently and preserves input/result association."
    - "A single batch failure does not cancel unrelated actions."
  artifacts:
    - path: "src/agentguard/runtime/engine.py"
      provides: "Lock-aware execute_batch API"
    - path: "tests/integration/test_batch_concurrency.py"
      provides: "End-to-end conflict and failure scenarios"
---

<objective>
Integrate resource locking at the Tool side-effect boundary and add explicit independent batch execution without changing the sequential Runtime loop.
</objective>

<tasks>

<task type="implementation">
  <name>Add structured resource-lock failures and lock-aware Tool execution</name>
  <files>src/agentguard/domain/results.py, src/agentguard/runtime/tool.py, src/agentguard/runtime/engine.py</files>
  <action>Add a resource-lock timeout/failure representation and wrap each ToolExecutor call with the injected ResourceLockManager. Acquire all declared resources before `TOOL_STARTED`, release in finally, and preserve existing permission, retry, timeout, and cancellation semantics.</action>
  <verify>Integration tests prove lock timeout does not call the Tool, normal/failing/timed-out/cancelled executions release locks, and no-policy sequential runs remain unchanged.</verify>
</task>

<task type="implementation">
  <name>Implement explicit execute_batch API</name>
  <files>src/agentguard/runtime/engine.py, src/agentguard/__init__.py</files>
  <action>Add `execute_batch(actions)` for independent flat CallTool Actions. Schedule batch members concurrently, preserve input-order association, isolate failures into individual results, and share the injected lock manager across Runtime instances when supplied. Reject Finish, malformed actions, duplicate unsupported dependency metadata, or invalid batch inputs before side effects.</action>
  <verify>Tests demonstrate non-conflicting overlap, conflicting serialization, independent success/failure results, input-order association, and no Router dependency.</verify>
</task>

</tasks>

<success_criteria>
- Resource protection occurs before Tool side effects.
- `execute_batch()` is concurrent, independent, deterministic in result association, and backward compatible with `run()`.
</success_criteria>
