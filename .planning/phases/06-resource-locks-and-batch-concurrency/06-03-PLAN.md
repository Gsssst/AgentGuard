---
phase: 06-resource-locks-and-batch-concurrency
plan: 03
type: implementation
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/agentguard/events/model.py
  - src/agentguard/reporting/report.py
  - src/agentguard/runtime/engine.py
  - src/agentguard/__init__.py
  - tests/unit/test_report.py
  - tests/integration/test_batch_concurrency.py
  - learning/zh-CN/06-resource-locks.md
  - learning/en/06-resource-locks.md
autonomous: false
must_haves:
  truths:
    - "Lock waits, lock timeouts, and batch outcomes are observable in structured evidence."
    - "Reports derive concurrency metrics from events rather than hidden counters."
    - "Learning notes distinguish local coordination from deferred distributed locking."
  artifacts:
    - path: "src/agentguard/reporting/report.py"
      provides: "Evidence-derived lock and batch metrics"
    - path: "learning/zh-CN/06-resource-locks.md"
      provides: "Chinese verified learning note"
    - path: "learning/en/06-resource-locks.md"
      provides: "English verified learning note"
---

<objective>
Make batch concurrency and lock behavior observable, measurable, and documented with deliberate failure evidence.
</objective>

<tasks>

<task type="implementation">
  <name>Add lock and batch event/report evidence</name>
  <files>src/agentguard/events/model.py, src/agentguard/reporting/report.py, src/agentguard/runtime/engine.py</files>
  <action>Add stable resource-wait, resource-lock-timeout, batch-started, and batch-finished event types or equivalent structured data while preserving existing event envelopes. Extend ReliabilityReport with lock waits/timeouts and batch result counters derived from events.</action>
  <verify>Reports count clean batches as zero failures, record lock timeout evidence, and never claim a Tool started when lock acquisition failed.</verify>
</task>

<task type="checkpoint">
  <name>Write bilingual concurrency learning notes</name>
  <files>learning/zh-CN/06-resource-locks.md, learning/en/06-resource-locks.md</files>
  <action>Document read/write/destructive locking, write priority, sorted acquisition deadlock prevention, lock timeout, independent batch semantics, explicit resource declarations, compatibility limits, and test evidence without claiming distributed locking or exactly-once behavior.</action>
  <verify>Both notes describe the same verified semantics and concrete test command/results.</verify>
</task>

</tasks>

<success_criteria>
- Lock waits, timeouts, and batch outcomes are visible in events and reports.
- Chinese and English learning notes match implementation and failure evidence.
</success_criteria>
