---
phase: 07-guardedtoolnode-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentguard/runtime/tool.py
  - src/agentguard/runtime/engine.py
  - src/agentguard/runtime/__init__.py
  - src/agentguard/__init__.py
  - tests/unit/test_runtime_explicit_tool.py
autonomous: true
requirements:
  - ADAPTER-05
  - ADAPTER-06
must_haves:
  truths:
    - Runtime can execute an explicitly supplied AgentGuard Tool without mutating its configured registry.
    - Explicit-tool execution applies permission, timeout, retry, resource-lock, and event semantics.
    - Existing Runtime.run() and execute_batch() behavior remains unchanged.
    - D-09: The Runtime is injected by the caller and reused rather than created per adapter call.
    - D-10: Adapter-owned Tools execute through Runtime controls without global registry mutation.
    - D-11: Same-name Runtime/Adapter conflicts are detected as configuration errors.
    - D-12: Explicit execution accepts a run identity and step suitable for audit and digest binding.
  artifacts:
    - path: src/agentguard/runtime/engine.py
      provides: explicit single-tool execution seam for adapters
    - path: tests/unit/test_runtime_explicit_tool.py
      provides: regression coverage for the seam and guard boundaries
  key_links:
    - from: src/agentguard/runtime/engine.py
      to: src/agentguard/runtime/tool.py
      via: shared ToolResult and Tool metadata execution path
    - from: explicit execution seam
      to: permission/resource/event boundaries
      via: same Runtime-owned policy and lock manager
---

<objective>
Create the narrow Runtime seam required for an Adapter-owned Tool registry.

Purpose: Phase 7 must not mutate a caller's global registry or bypass existing reliability controls. The seam lets the future `GuardedToolNode` supply a normalized Tool while Runtime remains responsible for policy and evidence.
Output: Explicit-tool execution API plus regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@.planning/phases/07-guardedtoolnode-foundation/07-CONTEXT.md
@src/agentguard/runtime/engine.py
@src/agentguard/runtime/tool.py
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add explicit Tool execution through Runtime controls</name>
  <files>src/agentguard/runtime/engine.py, src/agentguard/runtime/tool.py, src/agentguard/runtime/__init__.py, src/agentguard/__init__.py</files>
  <read_first>src/agentguard/runtime/engine.py, src/agentguard/runtime/tool.py, src/agentguard/runtime/permission.py, src/agentguard/runtime/resources.py</read_first>
  <action>Add a small public or adapter-facing Runtime method that accepts a `CallTool` and an explicit normalized `Tool` without registering it globally. Refactor shared execution logic only as needed so the method applies the injected Runtime's permission policy, lock manager, lock timeout, Tool timeout/retry semantics, and event sink. Preserve unknown-tool behavior for the existing registry path and avoid adding a second Runtime or executor.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_tool_execution.py tests/unit/test_permissions.py tests/unit/test_resource_locks.py tests/unit/test_runtime_explicit_tool.py</verify>
  <acceptance_criteria>
    - Explicit Tool execution returns the same `ToolResult` statuses and failure kinds as registered Tool execution.
    - A denied explicit Tool is not invoked and emits the existing permission event.
    - Existing full test suite remains green.
  </acceptance_criteria>
  <done>Runtime exposes a tested explicit-tool seam without global registry mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Test explicit seam failure boundaries</name>
  <files>tests/unit/test_runtime_explicit_tool.py</files>
  <read_first>tests/unit/test_tool_timeout.py, tests/unit/test_retry_policy.py, tests/unit/test_resource_locks.py, tests/unit/test_permissions.py</read_first>
  <action>Cover success, permission denial, timeout, retry exhaustion, resource-lock timeout, and event evidence using deterministic sync and async fake callables. Assert the explicit Tool is never inserted into or visible through the Runtime's original registry.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_runtime_explicit_tool.py</verify>
  <acceptance_criteria>
    - Test file contains deterministic assertions for all listed failure boundaries.
    - No test requires LangGraph or an external service.
  </acceptance_criteria>
  <done>Explicit execution seam has focused regression coverage.</done>
</task>
</tasks>

<verification>
- PYTHONPATH=src pytest -q tests/unit/test_runtime_explicit_tool.py
- PYTHONPATH=src pytest -q
- git diff --check
</verification>

<success_criteria>
- Adapter-owned Tools can be executed through Runtime-owned reliability controls.
- Existing registry and batch behavior is regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/07-guardedtoolnode-foundation/07-01-SUMMARY.md`.
</output>
