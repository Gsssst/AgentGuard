---
phase: 07-guardedtoolnode-foundation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - pyproject.toml
  - src/agentguard/integrations/__init__.py
  - src/agentguard/integrations/langgraph.py
  - src/agentguard/__init__.py
  - tests/unit/test_langgraph_adapter.py
autonomous: true
requirements:
  - ADAPTER-01
  - ADAPTER-02
  - ADAPTER-03
  - ADAPTER-04
  - COMPAT-01
must_haves:
  truths:
    - Core AgentGuard imports without LangGraph installed.
    - GuardedToolNode accepts LangChain-compatible tools and explicit ToolGuard metadata.
    - Missing ToolGuard configuration denies a call before invoking the underlying tool.
    - The node reads configurable state messages and returns a messages state update.
    - D-01: Messages default to state["messages"] with a configurable messages_key.
    - D-02: The node returns {"messages": [ToolMessage, ...]}.
    - D-03: Missing or empty messages produce a structured failure message.
    - D-04: Only the last AIMessage containing tool_calls is processed.
    - D-05: Tool invocation prefers ainvoke and falls back to threaded invoke.
    - D-06: Tool-call args are passed unchanged for LangChain schema validation.
    - D-07: Strings remain strings and other JSON-compatible values use stable JSON content.
    - D-08: Agent-visible errors are safe summaries while detailed exceptions remain in evidence.
  artifacts:
    - path: pyproject.toml
      provides: optional langgraph dependency extra
    - path: src/agentguard/integrations/langgraph.py
      provides: lazy-import GuardedToolNode and ToolGuard
    - path: tests/unit/test_langgraph_adapter.py
      provides: fake-tool and fake-message foundation tests
  key_links:
    - from: GuardedToolNode
      to: Runtime explicit-tool seam
      via: normalized CallTool and Tool execution
    - from: missing guard
      to: structured ToolMessage
      via: fail-closed pre-invocation decision
---

<objective>
Implement the optional LangGraph integration foundation and single-call node behavior.

Purpose: Make existing LangGraph users able to replace `ToolNode` with `GuardedToolNode` while keeping AgentGuard core lightweight and fail-closed.
Output: Packaging extra, adapter module, ToolGuard config, and deterministic fake integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@.planning/phases/07-guardedtoolnode-foundation/07-CONTEXT.md
@.planning/phases/07-guardedtoolnode-foundation/07-01-SUMMARY.md
@.planning/research/STACK.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add optional dependency and lazy adapter exports</name>
  <files>pyproject.toml, src/agentguard/integrations/__init__.py, src/agentguard/__init__.py</files>
  <read_first>pyproject.toml, src/agentguard/__init__.py, .planning/research/STACK.md</read_first>
  <action>Add a `langgraph` optional extra containing the tested LangGraph/LangChain Core range. Keep top-level `import agentguard` free of optional imports. Expose a clear lazy import path and actionable installation error when the adapter is imported without the extra.</action>
  <verify>PYTHONPATH=src python -c "import agentguard; print('core import ok')"</verify>
  <acceptance_criteria>
    - pyproject.toml contains a named langgraph optional extra.
    - Core import succeeds when langgraph is absent.
    - Adapter import error mentions `agentguard[langgraph]`.
  </acceptance_criteria>
  <done>Dependency boundary is explicit and core remains lightweight.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ToolGuard and single-call GuardedToolNode</name>
  <files>src/agentguard/integrations/langgraph.py</files>
  <read_first>src/agentguard/runtime/engine.py, src/agentguard/runtime/tool.py, src/agentguard/runtime/permission.py, .planning/phases/07-guardedtoolnode-foundation/07-CONTEXT.md</read_first>
  <action>Implement `ToolGuard` as a validated immutable configuration and `GuardedToolNode` with default `messages_key="messages"`. At invocation, locate only the last AIMessage containing tool_calls; for this phase process the single call path and return `{"messages": [ToolMessage]}`. Normalize LangChain tools through `.ainvoke()` first and threaded `.invoke()` fallback, pass args unchanged, create a CallTool and explicit AgentGuard Tool, use a RunnableConfig run ID when available or generate one, and return safe stable content with the original tool_call_id. Missing/empty messages and unconfigured tools must become structured failure messages without invoking the underlying tool. Detect name conflicts with the Runtime registry and raise a configuration error.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py</verify>
  <acceptance_criteria>
    - GuardedToolNode supports configurable messages_key and returns a messages state update.
    - Only the last tool-calling AIMessage is processed.
    - Underlying tool is not called when guard configuration is missing or permission denies it.
    - Success and failure ToolMessage values preserve tool_call_id and avoid stack traces.
  </acceptance_criteria>
  <done>Single-call adapter path is usable with deterministic fake LangChain-compatible objects.</done>
</task>
</tasks>

<verification>
- PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py
- PYTHONPATH=src pytest -q
- git diff --check
</verification>

<success_criteria>
- A configured LangChain-compatible tool executes through AgentGuard.
- Missing guard, malformed state, and tool failures produce structured messages.
</success_criteria>

<output>
After completion, create `.planning/phases/07-guardedtoolnode-foundation/07-02-SUMMARY.md`.
</output>
