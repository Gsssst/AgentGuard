---
phase: 07-guardedtoolnode-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/agentguard/integrations/langgraph.py
  - tests/integration/test_langgraph_optional.py
  - learning/zh-CN/07-langgraph-adapter.md
  - learning/en/07-langgraph-adapter.md
autonomous: true
requirements:
  - ADAPTER-05
  - ADAPTER-06
  - COMPAT-02
  - COMPAT-03
  - COMPAT-04
  - COMPAT-05
must_haves:
  truths:
    - Adapter results and AgentGuard events remain aligned for configured calls.
    - Fake tests cover the required success and failure boundaries without an LLM.
    - Real LangGraph tests run when optional packages are installed and otherwise skip clearly.
    - Bilingual learning notes document deliberate failures and known limitations.
  artifacts:
    - path: tests/integration/test_langgraph_optional.py
      provides: optional dependency integration smoke tests and clear skips
    - path: learning/zh-CN/07-langgraph-adapter.md
      provides: Chinese implementation and failure note
    - path: learning/en/07-langgraph-adapter.md
      provides: English implementation and failure note
  key_links:
    - from: GuardedToolNode
      to: ToolMessage
      via: stable success/failure translation
    - from: optional integration tests
      to: installed dependency detection
      via: pytest.importorskip with an actionable skip reason
---

<objective>
Complete Phase 7 evidence for the foundation adapter.

Purpose: The adapter is only valuable if its boundary is demonstrably compatible, failure-safe, and explainable.
Output: Optional real integration smoke test, expanded deterministic coverage, and bilingual learning notes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@.planning/phases/07-guardedtoolnode-foundation/07-CONTEXT.md
@.planning/phases/07-guardedtoolnode-foundation/07-02-SUMMARY.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Expand deterministic adapter failure coverage</name>
  <files>tests/unit/test_langgraph_adapter.py, src/agentguard/integrations/langgraph.py</files>
  <read_first>tests/unit/test_langgraph_adapter.py, tests/unit/test_tool_timeout.py, tests/unit/test_retry_policy.py, tests/unit/test_resource_locks.py</read_first>
  <action>Add focused tests for sync and async invocation, timeout, retry exhaustion, permission denial, lock timeout, unknown tool, malformed/empty state, stable JSON content, and safe error summaries. Fix only issues surfaced by these tests while preserving Phase 7 boundaries; do not add multi-call batching or interrupt/resume here.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py</verify>
  <acceptance_criteria>
    - All required Phase 7 failure categories have deterministic assertions.
    - No test invokes a real model provider.
  </acceptance_criteria>
  <done>Adapter foundation has complete deterministic evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional real integration smoke test</name>
  <files>tests/integration/test_langgraph_optional.py</files>
  <read_first>src/agentguard/integrations/langgraph.py, pyproject.toml</read_first>
  <action>Use clear import skipping when `langgraph` or `langchain_core` is unavailable. When installed, construct the smallest public LangGraph graph that feeds a tool-calling AIMessage to GuardedToolNode and assert the returned ToolMessage shape and call ID. Keep the test independent of external APIs.</action>
  <verify>PYTHONPATH=src pytest -q tests/integration/test_langgraph_optional.py</verify>
  <acceptance_criteria>
    - Missing dependencies produce a skipped test with an explicit reason.
    - Installed dependencies exercise public APIs only.
  </acceptance_criteria>
  <done>Compatibility evidence is honest about installed and uninstalled environments.</done>
</task>

<task type="auto">
  <name>Task 3: Record bilingual learning note</name>
  <files>learning/zh-CN/07-langgraph-adapter.md, learning/en/07-langgraph-adapter.md</files>
  <read_first>learning/zh-CN/06-resource-locks.md, learning/en/06-resource-locks.md, .planning/research/PITFALLS.md</read_first>
  <action>Document why LangGraph owns graph state/checkpoint, why the adapter fails closed, how the explicit Runtime seam avoids registry mutation, the deliberate failure scenarios, optional dependency behavior, and known Phase 8/9 limitations. Include commands and actual test evidence only.</action>
  <verify>test -s learning/zh-CN/07-langgraph-adapter.md && test -s learning/en/07-langgraph-adapter.md</verify>
  <acceptance_criteria>
    - Both notes mention deliberate failures and known limits.
    - Notes do not claim multi-tool batching or approval interrupt support is complete.
  </acceptance_criteria>
  <done>Learning evidence is available in both languages.</done>
</task>
</tasks>

<verification>
- PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py tests/integration/test_langgraph_optional.py
- PYTHONPATH=src pytest -q
- git diff --check
</verification>

<success_criteria>
- Phase 7 requirements are covered by deterministic tests and optional compatibility evidence.
- Bilingual notes accurately state what is implemented and deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/07-guardedtoolnode-foundation/07-03-SUMMARY.md`.
</output>
