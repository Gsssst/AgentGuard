---
phase: 08-multi-tool-batch-execution
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/agentguard/runtime/engine.py
  - src/agentguard/runtime/tool.py
  - tests/unit/test_runtime_explicit_tool.py
  - tests/integration/test_batch_concurrency.py
autonomous: true
requirements:
  - BATCH-02
  - BATCH-03
  - BATCH-05
must_haves:
  truths:
    - Adapter-owned Tools can be executed in a batch through Runtime permission, timeout, retry, resource-lock, and audit boundaries.
    - A per-batch positive-integer max_concurrency queues excess calls without adding a second Runtime-global limit or queue timeout.
    - Each batch item independently produces a ToolResult; one failure, timeout, cancellation, denial, unknown tool, or lock timeout does not cancel unrelated items.
    - Results preserve input order because aggregation retains the original item index.
    - D-06/D-07/D-08/D-09: max_concurrency is per node invocation, validates at initialization, and uses existing execution/lock timeouts.
    - D-14/D-15/D-16/D-17: resource coordination is delegated to ResourceLockManager with full release and safe lock-timeout metadata.
  artifacts:
    - path: src/agentguard/runtime/engine.py
      provides: explicit-tool batch execution seam with bounded per-batch concurrency
    - path: tests/integration/test_batch_concurrency.py
      provides: deterministic overlap, conflict, ordering, and failure-isolation evidence
  key_links:
    - from: Runtime explicit batch method
      to: ToolExecutor.execute_explicit
      via: normalized Tool supplied by adapter without registry mutation
    - from: each batch item
      to: ResourceLockManager.hold
      via: Runtime-owned lock acquisition before Tool side effects
---

<objective>
Extend the Runtime with the smallest explicit-tool batch seam needed by the LangGraph adapter. Preserve Phase 6 lock semantics and make concurrency, ordering, and failure isolation deterministic.
</objective>

<execution_context>
@.planning/phases/08-multi-tool-batch-execution/08-CONTEXT.md
@.planning/phases/06-resource-locks-and-batch-concurrency/06-CONTEXT.md
@src/agentguard/runtime/engine.py
@src/agentguard/runtime/tool.py
@src/agentguard/runtime/resources.py
@src/agentguard/domain/results.py
</execution_context>

<tasks>
<task type="implementation">
  <name>Add explicit Tool batch execution without registry mutation</name>
  <files>src/agentguard/runtime/engine.py, src/agentguard/runtime/tool.py</files>
  <read_first>src/agentguard/runtime/engine.py, src/agentguard/runtime/tool.py, src/agentguard/runtime/resources.py, src/agentguard/runtime/permission.py</read_first>
  <action>Introduce an adapter-facing batch method (or extend execute_batch with an explicit Tool/item form) that accepts CallTool plus normalized Tool pairs. Keep existing Runtime.execute_batch registry behavior compatible. Retain each entry's original input index and pass it as the external event step (equivalent to execute_explicit_tool(step=index)), so audit events remain tied to the originating call. Apply permission checks before lock acquisition, acquire all declared resources through the existing ResourceLockManager, invoke through ToolExecutor.execute_explicit, and emit the same external/batch events. Use an asyncio semaphore only when max_concurrency is set; do not add queue_timeout or a Runtime-global quota. Ensure each worker catches ordinary exceptions and converts them to ToolResult, while preserving cancellation handling so sibling tasks are not cancelled by one item.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_runtime_explicit_tool.py tests/integration/test_batch_concurrency.py</verify>
  <acceptance_criteria>
    - Adapter-owned Tools never enter the Runtime registry.
    - max_concurrency is scoped to one batch invocation and excess items wait for a slot.
    - Permission denial, unknown tool, approval-required-in-batch, timeout, retry exhaustion, and lock timeout remain per-item ToolResults.
    - Each batch item's events use its original input index as step.
    - ResourceLockManager remains the only conflict implementation and partial lock acquisition is released on failure.
  </acceptance_criteria>
  <done>Runtime exposes a tested explicit batch boundary with bounded independent execution.</done>
</task>

<task type="test">
  <name>Expand deterministic Runtime batch failure and concurrency tests</name>
  <files>tests/unit/test_runtime_explicit_tool.py, tests/integration/test_batch_concurrency.py</files>
  <read_first>tests/unit/test_runtime_explicit_tool.py, tests/integration/test_batch_concurrency.py, tests/unit/test_resource_locks.py, tests/unit/test_tool_timeout.py</read_first>
  <action>Add tests for adapter-owned explicit batch tools, per-batch concurrency peak, queueing, sibling completion after one failure/cancellation, input-order result tuples, same-resource read/write coordination, multi-resource partial-release on lock timeout, and safe failure kinds/attempt counts.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_runtime_explicit_tool.py tests/integration/test_batch_concurrency.py</verify>
  <acceptance_criteria>
    - Tests deliberately trigger at least one timeout, retry exhaustion, and lock timeout.
    - Tests prove unrelated calls still complete and no waiting tool is invoked after lock timeout.
  </acceptance_criteria>
  <done>Runtime batch semantics have focused regression and failure-injection evidence.</done>
</task>
</tasks>

<verification>
- PYTHONPATH=src pytest -q tests/unit/test_runtime_explicit_tool.py tests/integration/test_batch_concurrency.py
- PYTHONPATH=src pytest -q
- git diff --check
</verification>

<success_criteria>
- Explicit adapter tools execute concurrently within a per-batch limit while preserving Runtime reliability boundaries.
- Failures and lock conflicts are isolated to their own ToolResult and do not cancel unrelated calls.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-tool-batch-execution/08-01-SUMMARY.md`.
</output>
