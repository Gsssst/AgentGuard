---
phase: 08-multi-tool-batch-execution
plan: 02
type: implementation
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/agentguard/integrations/langgraph.py
  - tests/unit/test_langgraph_adapter.py
  - tests/integration/test_langgraph_optional.py
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02
  - BATCH-03
  - BATCH-04
  - BATCH-05
must_haves:
  truths:
    - One AIMessage with multiple tool_calls returns one ToolMessage per input call in strict input order.
    - Independent adapter calls overlap up to max_concurrency; calls sharing declared resources rely on Runtime/ResourceLockManager coordination.
    - D-01/D-02/D-03/D-04/D-05: malformed calls, duplicate IDs, unknown tools, and missing guards are isolated, never silently dropped, and use stable placeholder IDs when needed.
    - D-10/D-11/D-12/D-13: completion order cannot reorder results; unexpected exceptions and cancellations become per-call safe messages gathered from independent workers.
    - D-17: lock timeout ToolMessages expose only stable minimal error fields and no stack trace or sensitive arguments.
    - Phase 9 approval interrupt/resume remains untouched; approval-required calls in this batch retain the existing non-pausing structured failure behavior.
  artifacts:
    - path: src/agentguard/integrations/langgraph.py
      provides: multi-call normalization, bounded dispatch, ordered ToolMessage conversion
    - path: tests/unit/test_langgraph_adapter.py
      provides: deterministic malformed/unknown/denied/failure/order tests
    - path: tests/integration/test_langgraph_optional.py
      provides: real LangGraph multi-tool smoke coverage when optional dependencies exist
  key_links:
    - from: GuardedToolNode.__call__
      to: Runtime explicit batch seam
      via: adapter-owned normalized Tool entries and per-batch max_concurrency
    - from: ToolResult tuple
      to: ToolMessage list
      via: original index and original/stable tool_call_id mapping
---

<objective>
Turn the Phase 7 single-call GuardedToolNode into a multi-tool-call node while keeping LangGraph message ownership and AgentGuard execution ownership separate.
</objective>

<execution_context>
@.planning/phases/08-multi-tool-batch-execution/08-CONTEXT.md
@.planning/phases/07-guardedtoolnode-foundation/07-CONTEXT.md
@src/agentguard/integrations/langgraph.py
@src/agentguard/runtime/engine.py
@src/agentguard/domain/results.py
@tests/unit/test_langgraph_adapter.py
</execution_context>

<tasks>
<task type="implementation">
  <name>Normalize and dispatch every tool_call independently</name>
  <files>src/agentguard/integrations/langgraph.py</files>
  <read_first>src/agentguard/integrations/langgraph.py, src/agentguard/domain/actions.py, src/agentguard/domain/results.py</read_first>
  <action>Replace the Phase 7 len(calls)==1 guard with per-call normalization. Validate each call's mapping/name/args and derive a stable placeholder ID from its input index for missing or non-string IDs. Run a pre-dispatch duplicate-ID pass: every occurrence of a duplicated non-empty ID produces its own safe DuplicateToolCallId failure in its original position, none of those duplicate calls invokes a tool, and unique calls continue. Resolve existence before guard configuration: a name absent from the adapter tool registry is always UnknownTool; only a known tool lacking ToolGuard is PermissionDenied. Create these immediate structured ToolResult failures and include them in the same ordered aggregation. Dispatch only executable unique entries through the explicit Runtime batch seam with max_concurrency validated as a positive integer at node initialization. Convert every result to ToolMessage while preserving the mapped ID and safe content. Treat cancellation raised by an individual tool worker as that item's Cancelled result; do not indiscriminately swallow cancellation of the enclosing GuardedToolNode, which remains caller-controlled and must not leave detached sibling tasks. Keep Phase 9 approval interrupt/resume out of this path.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py</verify>
  <acceptance_criteria>
    - Every input call yields exactly one output ToolMessage in the same position.
    - UnknownTool and PermissionDenied never invoke underlying LangChain tools.
    - Success values use existing stable serialization; failures expose safe summaries with failure kind/attempts where available.
    - max_concurrency invalid values fail during GuardedToolNode construction.
  </acceptance_criteria>
  <done>GuardedToolNode supports ordered, bounded, independently failing multi-tool batches.</done>
</task>

<task type="test">
  <name>Cover multi-tool adapter behavior and real graph smoke path</name>
  <files>tests/unit/test_langgraph_adapter.py, tests/integration/test_langgraph_optional.py</files>
  <read_first>tests/unit/test_langgraph_adapter.py, tests/integration/test_langgraph_optional.py, tests/integration/test_batch_concurrency.py</read_first>
  <action>Add deterministic fake-tool cases for three-call input ordering despite varied durations, max_concurrency queueing, duplicate and invalid IDs, malformed args/name, unknown tool, missing guard, timeout, retry exhaustion, lock timeout, unexpected exception, and cancellation isolation. Assert an unknown name that has no guard is still UnknownTool, while a known adapter tool without a guard is PermissionDenied; neither invokes a tool. For duplicate IDs, assert both duplicate positions return DuplicateToolCallId failures and the underlying tool invocation count remains zero for those calls while unique calls still run. Test both cancellation cases: an individual worker cancellation becomes only that item's Cancelled message while siblings complete, whereas cancellation of the enclosing node propagates to the caller without leaking detached tasks. Add or extend an optional-dependency StateGraph smoke test proving multiple ToolMessages are appended with matching IDs; skip clearly when LangGraph is unavailable.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py tests/integration/test_langgraph_optional.py</verify>
  <acceptance_criteria>
    - Tests assert the exact ToolMessage order and one-to-one ID mapping.
    - Failure content does not expose secret arguments or exception stack traces.
    - Real graph test exercises the node only when optional dependencies are installed.
  </acceptance_criteria>
  <done>Phase 8 adapter behavior is reproducibly tested with fake tools and an optional real graph.</done>
</task>
</tasks>

<verification>
- PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py tests/integration/test_langgraph_optional.py
- PYTHONPATH=src pytest -q
- git diff --check
</verification>

<success_criteria>
- GuardedToolNode processes multiple AIMessage tool_calls concurrently within a per-call limit and returns ordered structured messages.
- All BATCH-01..05 outcomes, including malformed and failure cases, are safe, deterministic, and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-tool-batch-execution/08-02-SUMMARY.md`.
</output>
