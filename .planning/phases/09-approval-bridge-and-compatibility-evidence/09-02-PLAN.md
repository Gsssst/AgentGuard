---
phase: 09-approval-bridge-and-compatibility-evidence
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - tests/integration/test_langgraph_approval.py
  - tests/integration/test_langgraph_optional.py
  - tests/unit/test_langgraph_adapter.py
  - pyproject.toml
autonomous: true
requirements: [COMPAT-03, COMPAT-04]
must_haves:
  truths:
    - "D-13: clean-install evidence records only the actually verified LangGraph and LangChain Core versions and public APIs."
    - "D-14: the automated matrix includes all approval, tamper, redaction, and post-approval execution faults."
    - "D-16: optional integration tests skip explicitly when absent and execute when installed."
    - "A real compiled StateGraph with a checkpointer pauses on interrupt and resumes with Command(resume=...) using the same configurable.thread_id."
    - "Installed optional dependencies execute real approval tests; absent dependencies skip only those tests with an actionable agentguard[langgraph] message while core tests still run."
    - "Automated coverage demonstrates success, denial, missing decision, digest mismatch, argument tamper, missing tool on resume, redaction, timeout, retry exhaustion, lock conflict, and post-approval tool failure."
    - "The repository records the exact locally verified langgraph/langchain-core versions and uses a bounded optional dependency declaration consistent with clean-install evidence."
  artifacts:
    - path: "tests/integration/test_langgraph_approval.py"
      provides: "Real StateGraph + checkpointer + thread_id interrupt/resume evidence"
    - path: "tests/integration/test_langgraph_optional.py"
      provides: "Optional dependency skip behavior and existing real adapter smoke coverage"
    - path: "pyproject.toml"
      provides: "Documented bounded langgraph optional extra matching verified versions"
  key_links:
    - from: "tests/integration/test_langgraph_approval.py"
      to: "GuardedToolNode"
      via: "compiled graph invokes node with AIMessage and resumes using Command(resume=decision_map)"
    - from: "tests/integration/test_langgraph_approval.py"
      to: "langgraph.checkpoint.memory.MemorySaver"
      via: "same configurable.thread_id across paused and resumed invocations"
---

<objective>
Prove the approval bridge against deterministic fakes and the real installed LangGraph runtime, including optional-dependency behavior and all required fault classes.

Purpose: turn the public interrupt/resume contract into reproducible compatibility evidence without claiming an untested version matrix.
Output: unit/integration test coverage and dependency constraints backed by a clean-install run.
</objective>

<decision_coverage>
D-13: record only clean-install versions and public API evidence. D-14: exercise every approval and execution fault. D-16: explicit optional-dependency skip when absent and mandatory execution when installed.
</decision_coverage>

<execution_context>
@.planning/phases/09-approval-bridge-and-compatibility-evidence/09-CONTEXT.md
@.planning/phases/09-approval-bridge-and-compatibility-evidence/09-RESEARCH.md
@.planning/phases/07-guardedtoolnode-foundation/07-CONTEXT.md
@tests/integration/test_langgraph_optional.py
@tests/unit/test_langgraph_adapter.py
@pyproject.toml
</execution_context>

<interfaces>
The real tests must use public `StateGraph`, `START`/edges or equivalent public graph builders, `langgraph.checkpoint.memory.MemorySaver` (or the installed public in-memory checkpointer), `langgraph.types.Command`, and `configurable.thread_id`. They must not call private Pregel/checkpoint internals.

The adapter returns `{"messages": [ToolMessage, ...]}` and every message correlates through `tool_call_id`; approval decisions are mappings keyed by those IDs and include the digest generated in the interrupt projection.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic approval/fault matrix and optional skip assertions</name>
  <files>tests/unit/test_langgraph_adapter.py, tests/integration/test_langgraph_optional.py, tests/integration/test_langgraph_approval.py</files>
  <action>Per D-14 and D-16, extend fake-tool tests to cover approved and rejected calls, missing decisions (fail closed), digest mismatch, argument tampering, tool missing at resume, nested sensitive redaction, approved-tool timeout/retry exhaustion/lock conflict, and approved-tool execution failure. Assert one result per original call in input order, original IDs, no invocation for denied/mismatched calls, and unrelated calls continue after a failure. Add real integration tests guarded narrowly by importorskip with the exact install hint; when imports succeed they must run (never silently skip), and when absent the skip reason must name `agentguard[langgraph]` while core tests remain runnable.
  </action>
  <verify><automated>pytest -q tests/unit/test_langgraph_adapter.py tests/unit/test_langgraph_approval.py tests/integration/test_langgraph_optional.py tests/integration/test_langgraph_approval.py -rs</automated></verify>
  <done>All APPROVAL failure classes and Phase 8 reliability semantics have deterministic assertions; optional-dependency skip output is explicit and installed environments execute the real tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean-install public API compatibility and record bounded versions</name>
  <files>pyproject.toml, tests/integration/test_langgraph_approval.py</files>
  <action>Per D-13, create a clean temporary virtual environment, install the project with `.[langgraph]`, record `importlib.metadata` versions for langgraph and langchain-core, and run the real approval StateGraph tests. Update the optional dependency bounds in pyproject.toml only to the tested bounded range (or exact versions) and add a test assertion/report artifact that fails if the runtime public interrupt/Command/checkpointer APIs are unavailable. Also create a separate clean environment with only `pip install -e .` (no extras), run the core suite plus the real integration module, and assert the latter skips with an actionable `agentguard[langgraph]` reason rather than silently passing. Preserve core importability without extras. Capture both commands, skip output, and observed versions for Plan 03's bilingual evidence; do not promise historical versions or introduce a new package.</action>
  <verify><automated>phase9_tmp=$(mktemp -d) && python -m venv "$phase9_tmp/with-extra" && "$phase9_tmp/with-extra/bin/pip" install -e '.[langgraph]' && "$phase9_tmp/with-extra/bin/python" -c "import importlib.metadata as m; print(m.version('langgraph')); print(m.version('langchain-core'))" && "$phase9_tmp/with-extra/bin/pytest" -q tests/integration/test_langgraph_approval.py && python -m venv "$phase9_tmp/core-only" && "$phase9_tmp/core-only/bin/pip" install -e . && "$phase9_tmp/core-only/bin/pytest" -q tests/unit -rs && "$phase9_tmp/core-only/bin/pytest" -q tests/integration/test_langgraph_approval.py -rs</automated></verify>
  <done>Clean install succeeds, real StateGraph interrupt/resume tests execute with the same thread_id, observed versions are recorded, dependency bounds match evidence, and `import agentguard` still works without extras.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test AIMessage/resume fixtures → adapter | Fault fixtures intentionally exercise untrusted IDs, args and decisions. |
| Package resolver → test environment | Optional dependency versions influence public API compatibility. |
| Graph checkpointer → resumed node | Persisted state and thread IDs must not be confused with a second AgentGuard checkpoint. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-05 | Tampering | resume/argument fixtures | mitigate | Include explicit tamper and digest mismatch tests and require per-call denial. |
| T-09-06 | Information disclosure | test assertions/payload | mitigate | Assert secret markers never occur in serialized interrupt content or ToolMessage errors. |
| T-09-07 | Denial of service | lock/timeout/retry tests | mitigate | Bound every fake tool and assert independent calls complete despite one blocked/failed call. |
| T-09-SC | Tampering | package install | mitigate | Use only existing optional packages and verify registry-resolved versions in a clean venv before accepting bounds. |
</threat_model>

<verification>
Run the full suite (`pytest -q`) after the clean-install run and preserve `-rs` output demonstrating the intentional optional skip path when dependencies are absent. Confirm no test relies on private LangGraph modules.
</verification>

<success_criteria>
COMPAT-03 and COMPAT-04 are evidenced by passing deterministic and real graph tests, explicit skip behavior, and a reproducible clean-install command with observed versions.
</success_criteria>

<output>
Create `.planning/phases/09-approval-bridge-and-compatibility-evidence/09-02-SUMMARY.md` after execution.
</output>
