---
phase: 10-fix-langgraph-messagesstate-approval-result-replacement
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/agentguard/integrations/langgraph.py
  - tests/unit/test_langgraph_adapter.py
autonomous: true
requirements:
  - BATCH-04
  - APPROVAL-03
  - APPROVAL-06
must_haves:
  truths:
    - "D-01/D-03: a pending prepare projection contains machine-readable approval state but no user-visible ApprovalRequired ToolMessage."
    - "D-02: the only internal preparation key is `_agentguard_prepared`; no redundant approval boolean is introduced."
    - "D-04/D-07: a batch with no pending approvals returns final messages directly and routing needs only pending-state inspection."
    - "D-06: callers register the existing public prepare() and approval() methods directly; this plan adds no graph-node factory."
    - "D-05/D-08: ordinary __call__ behavior remains compatible, including structured results for non-approval calls."
    - "A pending mixed batch can resume through approval() and produce one final result per original call without changing Runtime permission, digest, lock, timeout, retry, or failure semantics."
  artifacts:
    - path: "src/agentguard/integrations/langgraph.py"
      provides: "Replay-safe prepare/approval state projection for MessagesState reducers"
    - path: "tests/unit/test_langgraph_adapter.py"
      provides: "Projection and compatibility regression assertions"
  key_links:
    - from: "GuardedToolNode.prepare"
      to: "_agentguard_prepared"
      via: "pending projection omits messages and carries direct/pending result data"
    - from: "GuardedToolNode.approval"
      to: "ToolMessage"
      via: "single final ordered projection after interrupt resume"
---

<objective>
Remove the approval placeholder message from the pending `GuardedToolNode.prepare()` projection while preserving the explicit two-node approval lifecycle and the legacy `__call__()` contract.
</objective>

<execution_context>
@.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-CONTEXT.md
@.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-PATTERNS.md
@.planning/v0.3-INTEGRATION-AUDIT.md
@.planning/phases/09-approval-bridge-and-compatibility-evidence/09-CONTEXT.md
@src/agentguard/integrations/langgraph.py
@src/agentguard/integrations/approval.py
@tests/unit/test_langgraph_adapter.py
</execution_context>

<tasks>
<task type="implementation">
  <name>Separate pending approval state from the messages projection</name>
  <files>src/agentguard/integrations/langgraph.py</files>
  <read_first>src/agentguard/integrations/langgraph.py, src/agentguard/integrations/approval.py, .planning/v0.3-INTEGRATION-AUDIT.md, .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-CONTEXT.md</read_first>
  <action>Change GuardedToolNode.prepare so a batch with any pending approval returns only the `_agentguard_prepared` mapping and does not return a `messages` key or create an ApprovalRequired ToolMessage. Preserve safe immediate results inside the mapping with their original input index and tool_call_id, preserve the original pending arguments/capabilities/run_id/digest binding for approval(), and keep the no-pending branch returning `{"messages": output}`. Update GuardedToolNode.approval to merge immediate, approved, denied, missing-decision, digest-mismatch, missing-tool, and execution-failure results into exactly one ordered ToolMessage per input index, then mark the prepared context consumed with an empty pending list in its returned state. Keep `__call__()` as the existing prepare-then-approval wrapper and do not add a node factory, reducer-specific private import, or second checkpoint store.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py -x</verify>
  <acceptance_criteria>
    - When prepare sees at least one pending call, `"messages" not in result` and no `ApprovalRequired` ToolMessage is constructed or returned.
    - The pending result contains `_agentguard_prepared.pending`, `run_id`, `batch`, `immediate`, and `calls_count`, and each stored item retains its original `input_index` and `tool_call_id`.
    - When no call is pending, prepare returns `messages` with one final result per input call and does not require approval routing.
    - approval returns one ToolMessage for every input index in ascending order, preserves each original tool_call_id, and returns a consumed prepared context whose pending list is empty.
    - Existing permission, action-digest, Runtime batch, resource-lock, timeout, retry, and structured error code paths are not bypassed.
  </acceptance_criteria>
  <done>Adapter state projection no longer appends approval placeholders under add_messages semantics, while final approval output remains complete and ordered.</done>
</task>

<task type="test">
  <name>Add focused projection and legacy-entry regression tests</name>
  <files>tests/unit/test_langgraph_adapter.py</files>
  <read_first>tests/unit/test_langgraph_adapter.py, src/agentguard/integrations/langgraph.py, .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-CONTEXT.md</read_first>
  <action>Add deterministic fake-tool tests for (1) a mixed pending prepare result that has no `messages` key and stores direct results in `_agentguard_prepared`, (2) a no-pending prepare result that returns final messages immediately, (3) ordinary `GuardedToolNode.__call__()` success/failure behavior without approvals, and (4) consumed pending state after approval. Assert exact tool_call_id values and structured error types without relying on exception text or private LangGraph internals.</action>
  <verify>PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py tests/unit/test_langgraph_approval.py</verify>
  <acceptance_criteria>
    - The pending projection test fails if an ApprovalRequired placeholder or any `messages` key is reintroduced.
    - The no-pending test proves prepare emits final messages directly and the legacy `__call__()` test proves a normal tool still executes once with its original ID.
    - Approval projection assertions prove exactly one result per input ID and an empty pending list after completion.
  </acceptance_criteria>
  <done>Unit tests lock the state/message separation and compatibility contract before real reducer integration testing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-01 | Tampering | `_agentguard_prepared` state | mitigate | Preserve original arguments, run ID, input index, capabilities, and digest; approval() reuses existing fail-closed validation. |
| T-10-02 | Information disclosure | pending state/message projection | mitigate | Do not expose approval placeholders or raw exception details; keep interrupt-facing arguments redacted and digest input unredacted only inside the existing bound context. |
| T-10-03 | Repudiation | result/message correlation | mitigate | Emit exactly one final ToolMessage per original tool_call_id and retain structured error types. |
| T-10-04 | Denial of service | stale approval route | mitigate | Mark the prepared context consumed with `pending: []` and do not hold resource locks while waiting. |
</threat_model>

<verification>
Run `PYTHONPATH=src pytest -q tests/unit/test_langgraph_adapter.py tests/unit/test_langgraph_approval.py`, inspect that the pending branch omits `messages`, and run `git diff --check`.
</verification>

<success_criteria>
The adapter can separate pending approval state from user-visible messages, preserve the legacy entry point, and produce a single ordered final result projection without changing Runtime execution boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-01-SUMMARY.md`.
</output>
