---
phase: 10-fix-langgraph-messagesstate-approval-result-replacement
plan: 02
type: test
wave: 2
depends_on:
  - 10-01
files_modified:
  - tests/integration/test_langgraph_approval.py
  - tests/unit/test_langgraph_adapter.py
  - .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.md
  - .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.en.md
autonomous: true
requirements:
  - BATCH-04
  - APPROVAL-03
  - APPROVAL-06
  - COMPAT-04
  - COMPAT-05
must_haves:
  truths:
    - "D-09/D-10: a real public MessagesState + add_messages graph pauses without an approval placeholder and resumes with exactly one ordered ToolMessage per original tool_call_id."
    - "D-03: direct, approval-required, and denied calls in one mixed batch remain isolated and are merged in original input order after resume."
    - "D-06: the integration graph registers public prepare() and approval() nodes directly and leaves conditional routing to the caller."
    - "D-11: regression scope is limited to the default messages reducer; no custom messages_key or multi-reducer matrix is added."
    - "D-12: no-pending prepare and legacy __call__ compatibility are protected by smoke coverage."
    - "The deliberate B1 reducer failure is reproduced as a regression test and the full suite remains green after the fix."
    - "Chinese and English learning records explain the failure, fix, evidence, and at-least-once/deferred limits without overstating support."
  artifacts:
    - path: "tests/integration/test_langgraph_approval.py"
      provides: "Real MessagesState/add_messages pause-resume regression"
    - path: ".planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.md"
      provides: "Chinese evidence-backed learning record"
    - path: ".planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.en.md"
      provides: "English structurally paired learning record"
  key_links:
    - from: "tests/integration/test_langgraph_approval.py"
      to: "GuardedToolNode.prepare/approval"
      via: "public StateGraph nodes with MessagesState reducer and MemorySaver checkpoint"
    - from: "Command(resume=...)"
      to: "ToolMessage.tool_call_id"
      via: "same configurable.thread_id and per-call digest-bound decisions"
---

<objective>
Prove the fix against the conventional LangGraph `MessagesState` reducer, preserve the existing compatibility smoke paths, and capture bilingual failure-oriented evidence for the blocker closure.
</objective>

<execution_context>
@.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-CONTEXT.md
@.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-PATTERNS.md
@.planning/v0.3-INTEGRATION-AUDIT.md
@.planning/v0.3-v0.3-MILESTONE-AUDIT.md
@.planning/phases/09-approval-bridge-and-compatibility-evidence/09-CONTEXT.md
@tests/integration/test_langgraph_approval.py
@tests/integration/test_langgraph_optional.py
@tests/unit/test_langgraph_adapter.py
@pyproject.toml
</execution_context>

<tasks>
<task type="test">
  <name>Add the real MessagesState/add_messages mixed-batch regression</name>
  <files>tests/integration/test_langgraph_approval.py, tests/unit/test_langgraph_adapter.py</files>
  <read_first>tests/integration/test_langgraph_approval.py, tests/integration/test_langgraph_optional.py, tests/unit/test_langgraph_adapter.py, src/agentguard/integrations/langgraph.py, .planning/v0.3-INTEGRATION-AUDIT.md</read_first>
  <action>Extend the existing optional integration module with a public `MessagesState`-based graph state that retains the `_agentguard_prepared` machine key and the standard `messages` reducer. Add one mixed-batch test containing a directly allowed call, an approval-required call, and a known call denied by missing guard; invoke with `MemorySaver`, inspect the paused result, resume with public `Command(resume=...)` on the same `configurable.thread_id`, and assert the paused messages contain no ApprovalRequired placeholder while the resumed state contains exactly one ToolMessage for each original ID in input order. Keep the existing redaction, digest mismatch, missing-tool, and direct-call-once coverage, and retain the two unit smoke tests from Plan 01. Do not add custom reducers, private Pregel/checkpoint APIs, or a second graph state store.</action>
  <verify>PYTHONPATH=src pytest -q tests/integration/test_langgraph_approval.py tests/integration/test_langgraph_optional.py tests/unit/test_langgraph_adapter.py -rs</verify>
  <acceptance_criteria>
    - The graph state uses the public `MessagesState`/`add_messages` behavior and compiles with the existing public `MemorySaver` checkpointer.
    - The paused result has one interrupt, no `ToolMessage` whose content/error is `ApprovalRequired`, and no pending approval tool invocation has occurred.
    - The resumed `messages` projection has one and only one ToolMessage per original call ID, with IDs and order exactly equal to the input tool_calls; direct, approved, and denied outcomes remain isolated.
    - The direct tool invocation count remains one across pause/resume, and the approved tool invocation count is one only after explicit approval.
    - The module still skips only when optional imports are unavailable, with an `agentguard[langgraph]` reason; installed environments execute the test.
  </acceptance_criteria>
  <done>The B1 failure is covered by a real reducer regression that passes after the projection fix.</done>
</task>

<task type="documentation">
  <name>Capture bilingual blocker-closure learning evidence and run release checks</name>
  <files>.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.md, .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.en.md</files>
  <read_first>.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-CONTEXT.md, .planning/v0.3-INTEGRATION-AUDIT.md, .planning/v0.3-v0.3-MILESTONE-AUDIT.md, tests/integration/test_langgraph_approval.py, .planning/phases/09-approval-bridge-and-compatibility-evidence/09-LEARNINGS.md, .planning/phases/09-approval-bridge-and-compatibility-evidence/09-LEARNINGS.en.md</read_first>
  <action>Write structurally paired Chinese and English learning records describing the observed B1 sequence (placeholder on pause, duplicate result on resume), the separation of `_agentguard_prepared` from `messages`, the real mixed-batch regression, direct-call replay evidence, structured denial/error behavior, exact test commands and results, and known limits. Include deliberate-fault evidence for approval/denial and the reducer duplication, reference Phase 10 decisions D-01 through D-12 where relevant, and state that compatibility remains bounded to the already verified LangGraph/LangChain versions with at-least-once side-effect semantics. Then run the full suite and `git diff --check`; record observed outcomes without copying secrets or stack traces.</action>
  <verify>PYTHONPATH=src pytest -q && git diff --check && test -s .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.md && test -s .planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-LEARNINGS.en.md</verify>
  <acceptance_criteria>
    - Both learning files contain matching sections for failure reproduction, root cause, fix, tests/evidence, compatibility boundary, and deferred limits.
    - The records state the exact full-suite and targeted integration commands used and their observed pass/skip result without claiming untested versions or exactly-once execution.
    - The full source-tree pytest command exits 0 and `git diff --check` exits 0 after the regression test is added.
  </acceptance_criteria>
  <done>Phase 10 has bilingual, auditable evidence that the reducer blocker is fixed and the full suite remains healthy.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-05 | Tampering | persisted LangGraph state/resume | mitigate | Use the same thread ID, preserve digest-bound original calls, and continue fail-closed per-call decisions. |
| T-10-06 | Information disclosure | interrupt and learning evidence | mitigate | Assert recursive redaction and write only safe summaries; omit raw secrets, exception stacks, and lock internals. |
| T-10-07 | Repudiation | duplicate/missing result correlation | mitigate | Assert exact one-to-one ID mapping and input order in the real reducer state after resume. |
| T-10-08 | Availability | replayed direct side effects | mitigate | Split prepare/approval nodes, assert direct invocation count remains one, and document at-least-once limits. |
</threat_model>

<verification>
Run the targeted real integration and unit suites with `-rs`, then run `PYTHONPATH=src pytest -q` and `git diff --check`. Review both learning files section-for-section and rerun the v0.3 milestone audit after execution to confirm B1 and its three affected requirements are no longer partial.
</verification>

<success_criteria>
The conventional `MessagesState + add_messages` graph pauses without placeholder results and resumes with one ordered result per original call, while compatibility evidence and bilingual learning records make the v0.3 blocker closure reproducible and honestly bounded.
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-langgraph-messagesstate-approval-result-replacement/10-02-SUMMARY.md`.
</output>
